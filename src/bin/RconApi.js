import fetch from "node-fetch"

export default function RconClient(){
  

  function connect({host,ip,port},password}){

  }

  function send({uid,command}){

  }

  function getResponse({uid,lastResponceUid}){

  }

  return {
    connect,
    send,
    getResponse
  }
}